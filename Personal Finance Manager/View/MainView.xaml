<Window x:Class="Personal_Finance_Manager.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Personal_Finance_Manager.View"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Login" Command="{Binding NavigateLoginCommand}" />
            <MenuItem Header="Register" Command="{Binding NavigateRegisterCommand}" />
            <MenuItem Header="Budget" Command="{Binding NavigateBudgetCommand}" />
        </Menu>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </DockPanel>
</Window>
